<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pal.doctive">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ProfileActivity"></activity>
        <activity android:name=".activities.CoachReadyActivity" />
        <activity android:name=".activities.SurveyCompletedActivity" />
        <activity android:name=".activities.SurveyActivity" />
        <activity android:name=".activities.SecondActivity" />
        <activity android:name=".activities.WorkoutrunningActivity" />
        <activity android:name=".activities.WorkoutdetailsActivity" />
        <activity android:name=".activities.WorkoutlistActivity" />
        <activity android:name=".activities.DayActivity" />
        <activity android:name=".activities.WeekActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>